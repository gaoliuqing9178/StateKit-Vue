这份文件用于继续交接当前仓库状态。先看根目录的 `CHANGELOG.md`，再看这里。

# 当前状态

- `@statekit-vue/shared@0.2.0` 已发布并已确认。
- `@statekit-vue/vue@0.2.0` 已发布并已确认。
- 当前公开口径已经切到“6 个 category-first 入口 + 18 个 preset recipes”。
- docs 站的主路由已经切到 `/recipes`，旧 `/blocks` 路由保留为兼容跳转。

# 这轮已经完成的事情

## 1. 公开 API 已切到类别优先

当前推荐的公开入口是：

- `EmptyState`
- `LoadingState`
- `ErrorState`
- `PermissionState`
- `UpgradeState`
- `SuccessState`

底层仍然保留 18 个 preset recipes。旧的场景名导出依然存在，但已经作为 deprecated compatibility exports 处理。

## 2. docs 站点主路由已切到 `/recipes`

- 新主路由：
  - `/recipes`
  - `/recipes/:slug`
- 兼容旧路由：
  - `/blocks`
  - `/blocks/:slug`

旧地址会跳转到新地址，避免历史链接失效。

## 3. docs 内部命名已从 `block*` 改成 `recipe*`

已经完成的重命名包括：

- `apps/docs/src/lib/recipe-components.ts`
- `apps/docs/src/lib/recipe-docs.ts`
- `apps/docs/src/views/RecipesView.vue`
- `apps/docs/src/views/RecipeDetailView.vue`

相关导入、计算属性、循环变量和代码片段生成函数也已经同步改成 `recipe*` 口径。

## 4. 发布文档已同步

这轮已经同步过：

- `README.md`
- `README.zh-CN.md`
- `CHANGELOG.md`
- `剩餘部分.md`
- `packages/vue/README.md`
- `packages/shared/README.md`

## 5. 版本线已发布到 `0.2.0`

当前已经对齐并发布：

- `packages/shared/package.json`
- `packages/vue/package.json`
- `apps/docs/package.json`
- `examples/vite-vue-admin/package.json`
- `package-lock.json`

其中：

- `@statekit-vue/shared`：`0.1.2` -> `0.2.0`
- `@statekit-vue/vue`：`0.1.2` -> `0.2.0`
- `@statekit-vue/vue` 对 `@statekit-vue/shared` 的依赖也已同步到 `0.2.0`

# 这轮已确认通过的校验

已重新执行并通过：

```bash
npm run typecheck
npm run build
npm run pack:check
npm run smoke:install
```

发布后也已经确认：

```bash
npm view @statekit-vue/shared version
npm view @statekit-vue/vue version
```

# 后续建议

如果下一轮继续收口，优先看这些方向：

1. 同步更新仍残留旧版本状态的内部文档，例如 handoff brief、launch checklist 和 docs QA 规范。
2. 人工浏览 docs 首页、`/recipes` 列表页、`/recipes/:slug` 详情页和三个 example 页，确认没有文案和交互回退。
3. 继续补充自动化测试或可视化回归，而不是继续堆新的 recipe 数量。

# 注意事项

- 当前仓库本来就是长期演进中的 monorepo，不要不加判断地回退已有改动。
- 如果后面继续改 docs 文案，优先同步 `README.md`、`README.zh-CN.md`、`CHANGELOG.md` 和 `packages/vue/README.md`，避免发布页与仓库首页再次分叉。
- 如果后面继续动 docs 站内部结构，优先检查 `apps/docs/src/router.ts`、`apps/docs/src/views/RecipesView.vue`、`apps/docs/src/views/RecipeDetailView.vue` 和示例页里的内部跳转。

To continue this session, run codex resume 019d615e-baf2-7020-9e7b-d857db0113b4