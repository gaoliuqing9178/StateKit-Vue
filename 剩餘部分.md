这份文件给下一个 AI 做直接交接。先读仓库根目录的 `CHANGELOG.md`，再读这里。

## 当前已完成

### 1. 发布面与仓库说明已经补齐并对齐

- 根 README、中文 README、包级 README、npm 元数据、根 `CHANGELOG.md`、handoff brief 和 launch checklist 当前口径已经统一。
- 根 README 与中文 README 已明确归入已完成项，后续只做维护与精炼；V1 仍明确为 18 个 Block / 6 个类别，onboarding 继续通过 `FirstProjectState` 这类 first-run empty state 承接。
- 包级可发布内容和 smoke install 流程此前已经补齐；当前对外 npm 包名已收口为 `@statekit-vue/shared` 与 `@statekit-vue/vue`。

### 2. CTA Action API 已完成

当前 `StateAction` 已支持：

- `label`
- `href`
- `disabled`
- `loading`
- `loadingLabel`
- `onClick`

同时：

- `primaryAction` / `secondaryAction` 现在支持 `null`
- `undefined` 继续沿用预设默认按钮
- `null` 用于显式移除默认按钮
- 共享 shell 已统一处理按钮 / 链接渲染、重复点击拦截、loading 文案和 disabled / loading 视觉状态

关键文件：

- `H:\StateKit\packages\shared\src\types.ts`
- `H:\StateKit\packages\vue\src\types.ts`
- `H:\StateKit\packages\vue\src\base\StatePresetBlock.vue`
- `H:\StateKit\packages\vue\src\base\StateBlockShell.vue`
- `H:\StateKit\packages\vue\src\styles\base.css`

### 3. Docs 的 Blocks 详情页已经重写

`apps/docs/src/views/BlockDetailView.vue` 已经从简单预览页改成详细用法页，当前覆盖内容包括：

- 组件适用场景
- 默认 layout / density / tone / 默认 CTA
- `title`、`description`、`tone`、`density`、`layout` 的传法
- `primaryAction`、`secondaryAction` 的传法
- 自定义按钮文案
- `onClick` 点击事件
- `href` 链接按钮
- `loading` / `loadingLabel`
- `disabled`
- `null` 移除默认按钮
- 直接传值、脚本变量绑定、`v-bind` 整对象三种传参方式

相关文件：

- `H:\StateKit\apps\docs\src\views\BlockDetailView.vue`
- `H:\StateKit\apps\docs\src\lib\example-code.ts`
- `H:\StateKit\apps\docs\src\styles.css`

### 4. Blocks 页面中宽度布局问题已纳入修正

当前 docs 样式已经补了：

- detail 区域的 `min-width: 0`
- 多档网格折叠
- detail prop / doc / guide 卡片的响应式列数调整

目标是避免在 1700px 到 1120px 这段宽度里出现 detail sections 被遮挡或裁切。

如果后面还要继续微调，优先从这里看：

- `H:\StateKit\apps\docs\src\styles.css`

### 5. Example 页面已按当前功能重写

docs 中的三个 example 页已改成真实交互场景，不再是旧的占位展示：

- `Admin Empty States`
- `Permissions And Upgrade`
- `Task Flow`

外部示例也已同步：

- `H:\StateKit\examples\vite-vue-admin\src\App.vue`
- `H:\StateKit\examples\vite-vue-admin\src\styles.css`

这些 example 现在明确展示：

- `onClick`
- `href`
- `loading`
- `loadingLabel`
- `disabled`
- `null` 移除次要按钮

### 6. `StateBlockShell` 的响应式问题已继续修正

这轮继续收口了共享 shell 的中等宽度问题：

- `page` 布局下 `sk-shell__actions` 已改为居中，避免 page 型状态块 CTA 明显贴左。
- `panel` 布局在 `1160px` 及以下会提前切成单栏，降低媒体区把标题和按钮挤出可视区的风险。
- `.sk-shell__content` 已补上 `min-width: 0`，用于避免 grid 子项在收缩时把内容裁掉。
- 当前这组修正主要覆盖了此前实际出现的 `1160px~1000px` 和 `920px~760px` 遮挡问题。

相关文件：

- `H:\StateKit\packages\vue\src\styles\base.css`

### 7. 文档说明已同步更新方向

如果继续改对外说明，优先同步这几类文件：

- 根 `README.md`
- 根 `README.zh-CN.md`
- 根 `CHANGELOG.md`
- `docs/README.md`
- `docs/statekit-ai-handoff-brief.md`
- `docs/statekit-launch-checklist.md`
- `docs/statekit-block-spec.md`
- `docs/statekit-visual-system.md`
- `docs/statekit-docs-site-and-qa-spec.md`

## 当前已验证

最近一次已在当前仓库状态下重新跑过：

```bash
npm run typecheck
npm run build
npm run pack:check
npm run smoke:install
```

四项都已通过。

补充说明：

- `typecheck` / `build` 已覆盖 `@statekit-vue/shared`、`@statekit-vue/vue`、`@statekit/docs` 和 `@statekit/example-vite-vue-admin` 四个 workspace。
- `pack:check` 已确认 `@statekit-vue/shared` 和 `@statekit-vue/vue` 的打包清单、README 与导出入口正常。
- `smoke:install` 已完成外部消费方安装与生产构建验证。
- `@statekit-vue/shared@0.1.0` 与 `@statekit-vue/vue@0.1.0` 已发布到 npm；后续再发新版时不能覆盖 `0.1.0`，需要先升级版本号。

## 当前剩余 / 下一步建议

当前用户明确提出的这批需求，文档口径和根级验证结果已经对齐。

如果下一轮还要继续做，建议按这个顺序：

1. 用浏览器人工检查 docs 的 block 详情页和 example 页。
2. 重点看这些宽度：1700、1440、1280、1160、1120、1000、920、760。
3. 如果后续继续改样式、文档或发布配置，再在根目录补跑：

```bash
npm run typecheck
npm run build
npm run pack:check
npm run smoke:install
```

4. 如果需要按清单推进，优先从 `docs/statekit-launch-checklist.md` 里仍未勾选的项开始；`TODO.md` 只保留后续执行项。

## 额外提醒

- 仓库现在本来就是 dirty worktree，不要随手回退已有改动。
- 如果再改 block 用法文档，优先同步 `BlockDetailView.vue` 和 `example-code.ts`，否则文案很容易再次和真实 API 脱节。
- `TODO.md` 已经整理了当前未完成项；如果后续继续收口发布前工作，优先维护这份文件而不是把状态散落到多个临时说明里。
